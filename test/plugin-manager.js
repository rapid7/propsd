/* eslint-env mocha */
'use strict';

const should = require('should');

describe('Plugin manager', () => {
  it('fetches the index from S3 and gets a list of sources', () => {

  });

  it('registers plugins with the storage engine', () => {

  });

  it('sets event handlers for plugin events', () => {

  });

  it('updates the storage engine when a plugin emits an update event', () => {

  });
});
